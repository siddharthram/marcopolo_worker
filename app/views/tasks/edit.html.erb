<h1>Task Detail</h1>

<div class="container">
	<div class="row imagediv">
		<%= form_for(@task, :remote => true) do |f| %>
		<div class="span6">
			<%= puts @task.imageurl %>
			<%= link_to image_tag(@task.imageurl), @task.imageurl,
			onclick: "window.open (@task.imageurl, 'Image','width=600,height=600')" %>
		</div>
		<div class="span4">
			<div class="output">
				<%= f.text_area :output, placeholder: "your work.."%>
			</div>
		</div>
	</div>
</div>
<%= f.submit "Post", class: "btn btn-large btn-primary"%>
		<%end%>

<%= link_to 'Show', @task %> |
<%= link_to 'Back', tasks_path %>
